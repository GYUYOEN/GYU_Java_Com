<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="fileMapper">
	<insert	id="insertData" parameterType="uploadFilesDto">
		INSERT INTO UPLOADFILES(ID, BID, FILENAME, LOCATION, URL, FILESIZE)
			VALUES(UPLOADFILES_SEQ.NEXTVAL, #{bId}, #{fileName}, #{location}, #{url}, #{fileSize})
	</insert>
	<select id="selectDatas" resultType="uploadFilesDto">
		SELECT ID, BID, FILENAME
			 , LOCATION, URL, FILESIZE
		  FROM UPLOADFILES
		 WHERE BID = #{bId}
	</select>
</mapper>
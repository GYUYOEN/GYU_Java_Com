<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="loginMapper">
	<insert id="saveUser" parameterType="empDto">
		INSERT INTO 
			TB_EMPLOYEES_TEST(emp_id
							, emp_nm
							, emp_pw
							, emp_check_pw
							, emp_email
							, emp_assist_email) 
					   VALUES(#{empId}
					        , #{empNm}
					        , #{empPw}
					        , #{empCheckPw}
					        , #{empEmail}
					        , #{empAssistEmail})
	</insert>
	<select id="getUserAccount" resultType="empDto">
		SELECT EMP_ID AS empId
		  FROM TB_EMPLOYEES
		 WHERE EMP_ID = #{empId}
	</select>
</mapper>